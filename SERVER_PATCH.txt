// Add this line immediately after the dbPool declaration closes (after "});"):
// app.locals.db = dbPool;
//
// Example placement in server.js:
//
// const dbPool = mysql.createPool({
//   host: process.env.DB_HOST || 'localhost',
//   ...
// });
// app.locals.db = dbPool;   // <-- ADD THIS LINE
//
// Then in the middleware section where req.db is set:
// app.use((req, res, next) => {
//   req.db = dbPool;
//   next();
// });
